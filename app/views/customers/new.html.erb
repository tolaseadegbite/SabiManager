<%= render 'layouts/modal_dialog' do %>
  <div data-action="turbo:submit-end->dialog#submitEnd">
    <h1 class="font-semibold text-xl sm:text-xl md:text-xl p-3 border-b">New customer</h1>
    <%= render "form", business: @business, customer: @customer %>
  </div>
<% end %>