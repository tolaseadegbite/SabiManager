<%= render 'layouts/modal_dialog' do %>
  <div data-action="turbo:submit-end->dialog#submitEnd">
    <h1 class="font-semibold text-xl sm:text-xl md:text-xl p-5 border-b">New product category</h1>
    <%= render "form", business: @business, product_category: @product_category %>
  </div>
<% end %>